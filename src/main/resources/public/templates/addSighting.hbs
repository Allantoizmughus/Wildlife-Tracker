{{#partial "content"}}
    <div class=""form>
        <form action="/sighting/add" method="post">
            <label>Select Animal</label>
            &emsp;<select name="name">
            {{#each animals}}
            <option value="{{name}}">{{name}} ID:{{id}}</option>
            {{/each}}
        </select>
            </br>
            <label>Location</label>
            &emsp;<input type="text" name="location">
            </br>
            <label>Your Name</label>
            &emsp;<input type="text" name="RangerName">
            </br>
            <button type="submit" class="btn">Submit Report</button>
            #end
        </form>
    </div>

{{/partial}}
{{>layout.hbs}}